---
role: company-pipeline-decision-index
status: active
authority: non-authoritative
update-boundary: Add one compact row when a durable Company/Pipeline decision is made. Do not expand an entry into full rationale, and do not edit the enforceable rule here — put it in the linked current authority.
---

# Decision Index

Compact index of durable decisions. This file is **not** the authority for
current rules: follow the linked contract or reference. The frozen expanded log
through ADR-0039 is available only through the
[historical catalog](../../../docs/history/README.md).

| ID | Decision | Reason | Status | Current authority |
| --- | --- | --- | --- | --- |
| ADR-0001 | Store a current-state snapshot, not event history. | The product tracks the present landscape. | Active | [Data Protocol](./README.md) |
| ADR-0002 | Reuse stable company, asset, and program IDs. | Stable identity enables updates and references. | Active | [Entities and Rows](./entities-and-rows.md) |
| ADR-0003 | Exclude stage and status from program identity. | They are mutable state. | Active | [Entities and Rows](./entities-and-rows.md) |
| ADR-0004 | Update mutable stage/status in place. | Progression must not fragment identity. | Active | [Entities and Rows](./entities-and-rows.md) |
| ADR-0005 | Judge source authority by field or claim type. | Different claims have different best sources. | Active | [Source and Entry Policy](./source-and-entry-policy.md) |
| ADR-0006 | Treat `N/A` as UI output only. | Missing data and display fallback are separate. | Active | [UI reference](../../app/docs/README.md) |
| ADR-0007 | Keep `null` distinct from `Unknown`. | They express absence versus confirmed unresolved state. | Active | [Source and Entry Policy](./source-and-entry-policy.md) |
| ADR-0008 | Retain evidenced discontinued programs. | Competitive history remains relevant to the current landscape. | Active | [Source and Entry Policy](./source-and-entry-policy.md) |
| ADR-0009 | Treat the original schema as provisional. | Early structural gaps required pilot evidence. | Superseded by ADR-0030 | [Data Protocol versioning](./README.md#versioning) |
| ADR-0010 | Defer speculative contract changes until pilot evidence. | Avoid designing unused schema. | Active principle | [Edge Cases](./edge-cases.md) |
| ADR-0011 | Generate deterministic operating aggregates. | Editable sources and consumer outputs need a clear boundary. | Active | [Generated Output Contract](./generated-output-contract.md) |
| ADR-0012 | Isolate the Ascletis stress-test fixture. | Diagnostic evidence must not enter production aggregates. | Superseded by ADR-0021 | [Historical catalog](../../../docs/history/README.md) |
| ADR-0013 | Back development stages with a registry. | Controlled values require consistent validation and ordering. | Active | [Source and Entry Policy](./source-and-entry-policy.md) |
| ADR-0014 | Separate regulatory state from development stage. | Jurisdictional milestones are not clinical phases. | Active | [Source and Entry Policy](./source-and-entry-policy.md) |
| ADR-0015 | Allow ISO 8601 partial evidence dates. | Sources often disclose only year or month precision. | Active | [Source and Entry Policy](./source-and-entry-policy.md) |
| ADR-0016 | Model an FDC/co-formulation as one combination asset. | A product configuration is distinct from co-administration. | Active | [Entities and Rows](./entities-and-rows.md) |
| ADR-0017 | Model independently administered products as a regimen. | A regimen is not a pipeline-program asset identity. | Active | [Entities and Rows](./entities-and-rows.md) |
| ADR-0018 | Store program/regimen company relationships and rights. | Licensing and co-development affect responsibility. | Active | [Entities and Rows](./entities-and-rows.md) |
| ADR-0019 | Keep one principal `companyId`. | The company-local model avoids an unresolved global graph. | Active | [Entities and Rows](./entities-and-rows.md) |
| ADR-0020 | Back relationship roles with a registry. | Role vocabulary needs consistent validation. | Active | [Source and Entry Policy](./source-and-entry-policy.md) |
| ADR-0021 | Preserve Ascletis only as historical diagnostic evidence. | The initial pilot is neither canonical nor golden validation data. | Active | [Historical catalog](../../../docs/history/README.md) |
| ADR-0022 | Keep Company/Pipeline internal references company-local. | Cross-company resolution is outside Contract 1.2. | Active | [Entities and Rows](./entities-and-rows.md) |
| ADR-0023 | Use `configurationKey` only for stable official regimen distinctions. | Trial-arm dosing must not create regimen identity. | Active | [Entities and Rows](./entities-and-rows.md) |
| ADR-0024 | Store the most advanced official stage and annotate its basis/state. | Regulatory milestones must not be approximated as phases. | Active | [Source and Entry Policy](./source-and-entry-policy.md) |
| ADR-0025 | Freeze the original v1 baseline. | Initial pilots supported the then-current model. | Superseded by ADR-0030 | [Historical catalog](../../../docs/history/README.md) |
| ADR-0026 | Define Scope v1.1 as the obesity/incretin landscape. | The product is broader than GLP-1 RA but not all obesity pharmacotherapy. | Superseded by ADR-0052 (preserved unmodified as the Scope 2.0 R2 path) | [Data Protocol scope](./README.md#dataset-scope) |
| ADR-0027 | Separate generic company routing from explicit clinical intent. | Clinical extraction needs a distinct workflow. | Refined by ADR-0035 | [Agent entry point](../../../AGENTS.md#research-routing) |
| ADR-0028 | Establish Clinical Evidence as a separate domain. | Study/result semantics differ from pipeline tracking. | Refined and partially superseded | [Clinical Evidence contract](../../clinical-evidence/docs/README.md) |
| ADR-0029 | Define the initial obesity-result semantic contract. | Clinical results require explicit Study/Arm/Endpoint/Outcome boundaries. | Superseded in shape by ADR-0037/0039 | [Clinical Evidence contract](../../clinical-evidence/docs/README.md) |
| ADR-0030 | Adopt Company/Pipeline Contract 1.1. | Pilot evidence justified sharper identity, aliases, rows, and source rules. | Superseded by ADR-0053 (Contract 1.2 adds `scopeClass` only; no other Contract 1.1 shape changed) | [Data Protocol](./README.md) |
| ADR-0031 | Harden aliases and status/operational-state validation. | Single sourcing and defensive checks prevent drift. | Active | [Entities and Rows](./entities-and-rows.md), [Source Policy](./source-and-entry-policy.md) |
| ADR-0032 | Require explicit study classification and sufficient row-merge evidence. | Equal mutable state alone does not prove one program. | Corrected by ADR-0033 | [Entities and Rows](./entities-and-rows.md) |
| ADR-0033 | Separate intervention model from protocol structure and tighten regimen tests. | Platform structure and background therapy were being conflated. | Active | [Entities and Rows](./entities-and-rows.md) |
| ADR-0034 | Close Clinical preflight documentation and validator gaps. | Readiness required minimal semantic-duplicate and comparison safeguards. | Active implementation background | [Clinical Evidence contract](../../clinical-evidence/docs/README.md) |
| ADR-0035 | Activate Clinical Evidence routing with Company/Pipeline first. | Clinical research depends on a current authoritative asset list. | Superseded by ADR-0061 | [Agent entry point](../../../AGENTS.md#research-routing) |
| ADR-0036 | Formalize estimand, population, and source-reported-result handling. | Pilot data exposed semantic-key and inference risks. | Active | [Clinical Evidence contract](../../clinical-evidence/docs/README.md) |
| ADR-0037 | Migrate Clinical Evidence to v2 with analysis groups and structured results. | Demonstrated pilot gaps required an atomic schema migration. | Superseded in version by ADR-0039; semantics retained | [Clinical Evidence contract](../../clinical-evidence/docs/README.md) |
| ADR-0038 | Harden Clinical v2 version fields and numeric results. | Canonical and projection versions needed explicit separation. | Refined by ADR-0039 | [Clinical Evidence contract](../../clinical-evidence/docs/README.md), [Generated Output Contract](./generated-output-contract.md) |
| ADR-0039 | Adopt Clinical Evidence v3 Study inventory. | Verified studies must be representable before outcomes exist. | Active | [Clinical Evidence contract](../../clinical-evidence/docs/README.md) |
| ADR-0040 | Preserve curated source-record order in generated Clinical Evidence output. | An `id` tie-breaker discarded authored clinical curation such as dose-ascending arms. | Active | [Generated Output Contract](./generated-output-contract.md) |
| ADR-0041 | Re-evaluate a comparison family atomically when a follow-up source re-reports it, and exhaust the search before declaring non-disclosure. | A follow-up publication was applied to only part of one CT-388 placebo-anchored family, and an unexhausted search was recorded as non-disclosure. | Active | [Clinical Evidence contract](../../clinical-evidence/docs/README.md), [Clinical Evidence workflow](../../clinical-evidence/docs/workflow.md) |
| ADR-0042 | Own Study family as an explicit authored field and group Asset Clinical Detail by it. | Acronym-derived grouping would invent series from asset codes and protocol numbers. | Active | [Clinical Evidence contract](../../clinical-evidence/docs/README.md), [UI Reference](../../app/docs/README.md) |
| ADR-0043 | Back mechanism grouping with an authored family registry keyed by exact mechanism text, classified by target and pharmacologic action. | The registry exhaustively maps stored mechanism strings and additionally defines authored regimen-only combination families, yielding 27 families in total. Substring matching would merge `GLP-1R agonist` with `GLP-1R agonist/GIPR antagonist`. Resolves the deferred mechanism-vocabulary question without rewriting stored wording. | Active | [Source and Entry Policy](./source-and-entry-policy.md#mechanism-formulation-and-platform), [Entities and Rows](./entities-and-rows.md) |
| ADR-0044 | Adopt Clinical Evidence 3.1 with an optional authored `Study.populationProfile` on four closed axes. | Deterministic population handling needs structure, but `population` is free text that must not be parsed; one enum could not represent independently combining axes (region, diabetes status, age, treatment context). All-or-nothing so a partial profile cannot read as permissive. | Active | [Clinical Evidence contract](../../clinical-evidence/docs/README.md) |
| ADR-0045 | Accept 10-of-15 Efficacy Comparison population coverage and freeze it behind `npm run data:probe:efficacy-population-coverage`. | Correctness over coverage: liraglutide then reported weight in kg only; cagrilintide and UBT251 state no diabetes criterion; MariTide phase 2 and CT-996-201 span a non-diabetic and a T2D cohort. Relaxing `mixed` or `not-specified` to non-diabetic would mix populations the sources never claimed. | Refined by ADR-0058 | [Clinical Evidence contract](../../clinical-evidence/docs/README.md#deferred-limitations) |
| ADR-0046 | Add optional `Study.seriousAdverseEventIncidence`, `nauseaVomitingIncidence`, and `antiDrugAntibodyIncidence` fields, required whenever a cited source reports them. | The prior "concise safety summary only" scope let SAE, GI tolerability, and immunogenicity incidence go unrecorded even when directly disclosed, since only free-text `safetySummary` existed to hold them. Concise per-fact fields close that gap without reintroducing exhaustive per-arm/per-event adverse-event modeling. Additive optional string fields, same precedent as `studyFamily`, so the schema version does not change. | Superseded by ADR-0048 | [Clinical Evidence contract](../../clinical-evidence/docs/README.md) |
| ADR-0047 | Replace the exhaustive per-result disposition requirement (every disclosed result individually entered/excluded/deferred/schema-boundaried) with a four-category completion check — primary/co-primary, central/key-secondary, headline responder, concise safety — cross-referenced against the highest-priority reviewed source. | The prior rule demanded an unbounded per-result audit ledger with no dedicated tool to carry it, in practice producing ad hoc, non-authoritative audit artifacts outside the canonical data flow. The narrower check preserves the actual goal (canonical does not silently miss a core result) without requiring exhaustive extraction and classification of every disclosed result or supplement. Comparison-family re-evaluation and `NO_PUBLIC_RESULTS`/`RESULT_AVAILABILITY_UNRESOLVED` search-exhaustion (ADR-0040, ADR-0041) are unaffected — they govern discovery completeness and family consistency, not per-result disposition coverage. | Active | [Clinical Evidence contract](../../clinical-evidence/docs/README.md), [Clinical Evidence workflow](../../clinical-evidence/docs/workflow.md) |
| ADR-0048 | Replace ADR-0046's three free-text `Study` safety-incidence fields with a closed set of four named safety Endpoints (`"Serious adverse events"`, `"Nausea"`, `"Vomiting"`, `"Anti-drug antibodies"`, `role`/`domain`: `"safety"`) carrying ordinary arm-level Outcomes, when a cited source directly reports a per-arm breakdown. | The free-text fields discarded per-arm granularity the cited source (a trial registry) already reported as individual arm counts, compressing it into an approximate range. `role`/`domain: "safety"` already existed in the schema and was unused across the entire dataset. Reusing the existing Endpoint/Outcome shape gives exact per-arm values and free Endpoints & outcomes UI support (clustering, filtering) instead of a bespoke summary card, while the fixed four-name set keeps this from reopening exhaustive per-event adverse-event modeling. `Study.safetySummary` is unaffected and remains the concise narrative fallback. | Active | [Clinical Evidence contract](../../clinical-evidence/docs/README.md) |
| ADR-0049 | Gate the Clinical Evidence completion check on the highest-priority *known* source's confirmed access, not just what was already reviewed; add per-result fallback equivalence (endpoint, timepoint, analysis unit/comparison, population, estimand); make a blocked source case-scoped (`FULL`/`FULL_WITH_FALLBACK`/`PARTIAL`) instead of halting the run; and move Clinical Evidence result-source priority fully out of Company/Pipeline. | An unreachable top-tier source (e.g. a paywalled NEJM full text) was being conflated with "no result exists," and one blocked source risked halting an entire company's run instead of only the affected result. Company/Pipeline separately listed a clinical-results source order, duplicating an authority Clinical Evidence already owns. | Active | [Clinical Evidence workflow](../../clinical-evidence/docs/workflow.md), [Clinical Evidence contract](../../clinical-evidence/docs/README.md), [Source and Entry Policy](./source-and-entry-policy.md#clinical-results) |
| ADR-0050 | Rename Company/Pipeline candidate disposition to STORED/EXCLUDED/DEFERRED, add an explicit zero-undispositioned GO/NO-GO gate, require row-scoped stage/status evidence, scope the official-pipeline reconciliation to Scope v1.1-qualifying or plausibly-qualifying candidates, and require stacked-branch re-validation against the cumulative working tree (allowing an intentional, reported evidence-based supersession of a prior correction, not just accidental loss). Add two generic validator guards: `metadata.sources` must be non-empty, and every `relationships[]` entry requires non-empty `sourceUrls`. | Recent company audits (Gan & Lee, Sciwind, Pfizer, AbbVie) shipped a missing pipeline indication, a sibling row's stage bleeding into a different row, a stale development code, a single-secondary-source discontinuation claim, and an under-sourced licensing effective date — all while the prior coverage gate's language was satisfied. The gate lacked an explicit zero-count check and the schema allowed a relationship or record with zero cited sources. A hard multi-source minimum for `codeName` and relationship `effectiveDate` was evaluated and rejected: it would fail nearly the entire existing single-sourced dataset and is better enforced as a research-time cross-check (documented in Source and Entry Policy) than a blocking validator. A `development.status: "Discontinued"` minimum-source-count guard was also tried and reverted: source count does not establish that a source directly supports the discontinuation claim — a single primary statement outweighs two sources that never make the claim — so this is a research-completion gate responsibility, not a mechanical validator rule. | Active | [Research Workflow](./research-workflow.md), [Entities and Rows](./entities-and-rows.md), [Source and Entry Policy](./source-and-entry-policy.md) |
| ADR-0051 | Separate population/comorbidity from indication: a disease named only as an enrolled population, comorbidity, eligibility criterion, or stratification factor is not an indication, and a population difference alone creates no program row. Split EXCLUDED (confirmed not a subject of this run) from DEFERRED (unresolved and re-enterable), with precedence that a Scope-designated `Defer` class is DEFERRED and never EXCLUDED. Add an indication-attribution research-completion check and the advisory `npm run data:probe:indication-scope` semantic audit. | Viking VK2735 (VANQUISH-2) and Structure aleniglipron (ACCOMPLISH-2) each had a weight-management trial's enrolled type-2-diabetes population promoted to a separate T2D indication row, and both needed a later merge. The prior wording allowed it: nothing said an eligibility criterion is not a development objective, and "insufficiently evidenced" was a valid EXCLUDED reason, which let Scope-deferred classes be closed out as excluded. The audit stays advisory rather than a validator rule because only sponsor material settles whether a second row is a distinct program — a population-derived row is perfectly schema-valid, so a hard check would fail legitimate splits such as semaglutide's approved obesity, T2D, and MASH rows. | Active | [Source and Entry Policy](./source-and-entry-policy.md#population-and-comorbidity-versus-indication), [Entities and Rows](./entities-and-rows.md#row-splitting), [Research Workflow](./research-workflow.md) |
| ADR-0052 | Adopt Scope 2.0 as an additive expansion of Scope v1.1: keep the existing core-mechanism qualification path (R2) exactly as defined in ADR-0026, unchanged in breadth, and add a second, mechanism-independent obesity-purpose qualification path (R1) alongside it. An asset qualifies under R1 ∨ R2. A qualifying Program triggers asset-scoped closure for its focal asset; a qualifying Regimen qualifies only itself and never propagates qualification, or asset-scoped closure, to its component assets — a component asset must independently satisfy R1 (via its own qualifying Program) or R2. | The dataset already carried four assets held in scope only by core mechanism, with no obesity-indication row (Roche CT-868, QL Biopharm ZT003, Novo Nordisk IcoSema, Gan & Lee GZR102); removing or narrowing R2 would drop them or force a retroactive re-justification. Treating R2 as additive rather than superseded preserves early asset-scoped discovery: a newly disclosed GLP-1/GIP asset still qualifies the moment its core mechanism is confirmed, before an obesity indication is ever disclosed. Regimen-to-component propagation was evaluated and rejected: a regimen is a co-administration configuration, not a component's own development program, and letting it qualify a component would pull that component's entire unrelated pipeline into closure (for example, a partner antibody co-dosed with tirzepatide in one regimen). | Active | [Data Protocol scope](./README.md#dataset-scope), [Entities and Rows](./entities-and-rows.md#scope-class) |
| ADR-0053 | Adopt Company/Pipeline Contract 1.2: add an authored, registry-backed `scopeClass` field to `PipelineProgramRecord` and `RegimenRecord`, naming the row's own position in the obesity landscape — `obesity-treatment`, `obesity-adjunct`, `obesity-comorbidity`, `metabolic-adjacent`, or `non-metabolic` — independent of `indications` free text. The registry's `obesityPurposeQualifying` flag marks only which classes express an obesity-purpose development objective (feeding Scope 2.0's R1); it does not mean dataset membership, asset qualification, Clinical Evidence eligibility, or UI visibility. `scopeClass` is never part of program/regimen identity. Contract 1.2 and Scope 2.0 became the active version in the commit where every existing Program and Regimen was backfilled with a source-supported authored `scopeClass` and the validator began requiring the field — see the required-field migration gate. | Asset-scoped closure produces a large non-obesity tail (T2D, MASH, cardiovascular, and unrelated indications on already-qualified assets) that `indications` free text cannot classify without parsing prohibited by the mechanism-family precedent, and that consumers could previously read only by inference, which `generated-output-contract.md` already had to guard against. `complication` was rejected for the comorbidity class because it asserts an unevidenced causal claim the dataset does not support; `comorbidity` states only the observable fact that obesity/overweight is the row's required development context. | Active | [Data Protocol scope](./README.md#dataset-scope), [Entities and Rows](./entities-and-rows.md#scope-class), [Generated Output Contract](./generated-output-contract.md) |
| ADR-0054 | Add the advisory `npm run data:probe:registry-citations` audit and a Clinical Evidence workflow preflight step that reuses it: it reports where a Company/Pipeline-stored direct ClinicalTrials.gov locator and a Clinical Evidence Study already correspond, so a Clinical Evidence run can reopen a known source instead of rediscovering it from a blank search. Company/Pipeline Research must still complete first in the same run; a stored locator never substitutes for that refresh. The audit does not edit data or decide Study inclusion, focal anchor, completeness, result availability, or result provenance — those stay Clinical Evidence's own decisions, and a locator may support a claim only after Clinical Evidence directly reopens and reviews it. Live-data unmatched, ambiguity, multi-company, or unparsed findings never fail it; an unknown `--company`, invalid arguments, a self-check failure, a parser-contract violation, or a source-read error do. `scopeClass` remains display-only, and a compact `--company` run reports that company's own matched-locator count, never the repository-wide total. No schema, operating data, or generated-output change. | Roughly half of Clinical Evidence's stored Studies corresponded to a registry record Company/Pipeline had already cited in its own operating data, but nothing indexed that correspondence, so Clinical Evidence rediscovered the same trial registry record from scratch every run. A persisted cross-domain locator or disposition ledger was evaluated and rejected for this change: `generated-output-contract.md` §8 and `edge-cases.md`'s "Investigation classification tracking" already defer a persisted candidate/disposition ledger, and CP row co-location is a focal-anchor candidate only, never an authoritative mapping (three of the stored locators are cited by more than one Company/Pipeline row). | Refined by ADR-0061 (same-run refresh requirement removed; probe remains advisory) | [Clinical Evidence workflow §Registry-citation preflight](../../clinical-evidence/docs/workflow.md#registry-citation-preflight-adr-0054) |
| ADR-0055 | Add a Program/Study/Regimen/add-on disposition order to Company/Pipeline research: classify a candidate on official sponsor evidence alone; treat enrolled population, trial design, dose scheme, maintenance-versus-initial treatment, and comparator as non-discriminators; consult an existing Clinical Evidence Study anchor only as a non-authoritative consistency check after independent classification, never as identity authority; and use a `DEFERRED_SCHEMA_CASE` reason when a sponsor explicitly asserts a concurrent Program the current contract cannot represent, rather than forcing it into a Study. Scope registry-citation evidence in the source policy to a row's stage and status, not to justifying a new Program by itself. Add the advisory `cited-registry-record-anchored-to-other-row` signal to `npm run data:probe:registry-citations`, reporting — never auto-resolving — a CP row citing a locator Clinical Evidence anchors to a different row. Document `technical.mechanism` (pharmacologic meaning, expected to agree across an asset's rows) and `technical.platform` (formulation-level, may legitimately differ) as distinct authorship levels; add no validator assert for either. This clarifies Contract 1.2 entry semantics — consistent with ADR-0050 and ADR-0051, which changed comparable or larger entry-semantics scope without a Contract version change — and does not add a field, change generated-output shape, or invalidate an existing row. | An Eli Lilly refresh (unmerged branch) created two Program rows for trials already modeled as Clinical Evidence Studies of an existing Program, deferred two more on the same error, and dropped a legitimate Study-anchored row on a misread collision — one root cause: no step asked whether a candidate was already a Study of an existing Program. The new `cited-registry-record-anchored-to-other-row` signal, run against `main`, found two pre-existing legitimate cases that the disposition order must not disturb: Roche CT-996's two Programs (obesity, T2D) share one background Phase 1 Study citation while each anchors its own Phase 2 Study; an Eli Lilly monotherapy Program cites the registry record for a combination Regimen's trial as supporting background evidence. Both are correctly reported as advisory, not auto-merged. A `technical` exact-consistency assert was evaluated and rejected: three Ascletis `platform` values differ exactly at the oral-versus-parenteral boundary the field is documented to record, and one Sciwind `mechanism` wording pair already resolves to the same family, so a raw-string assert would reject legitimate data before ever reaching this ADR's actual defect. | Active | [Research Workflow](./research-workflow.md), [Entities and Rows](./entities-and-rows.md#row-splitting), [Source and Entry Policy](./source-and-entry-policy.md), [Data Protocol versioning](./README.md#versioning) |
| ADR-0056 | Close the Phase B governance investigation (CP Contract/Scope/Workflow; CE Schema/Workflow/Projection; cross-domain alignment) with a Conditional GO: execute only safe, additive alignment fixes, and make no structural relocation of any domain-root README. Fixed: `decision-log.md`'s broken table (ADR-0042 onward were rendering as plain paragraphs, not table rows, from a stray blank line) and its missing frontmatter; `docs/history/README.md`'s 7 dead "Current authority" links (pre-migration path layout); archived `edge-cases.md`'s completed "Module B" Ascletis/Zealand worked examples to `docs/history/`, generalizing its two still-live concerns ("Field-level provenance", "Investigation classification tracking") as ordinary edge-case rows; closed 3 npm-script-to-doc coverage gaps (`data:probe:mechanism-families`, `data:probe:efficacy-comparison`, `data:probe:efficacy-population-coverage`); fixed a stale "Module 5" migration reference; resolved Clinical Evidence's Contract-versus-Schema terminology ambiguity with one clarifying note plus one heading fix, without a mass rename; reduced a Company/Pipeline-rule paraphrase in Clinical Evidence's workflow.md to a link, and cross-referenced ADR-0055's anchor-consistency principle as Clinical Evidence's own mirror-image rule; replaced every bare "v1"/"v2" instance in currently-active Company/Pipeline docs with its precise current meaning (`future contract/schema backlog`, `Stable under Contract 1.2`, `current Contract 1.2 behavior`, `Under Contract 1.2`), leaving intact every specific historical version reference (Contract 1.1, Scope v1.1, Clinical Evidence Schema v2.0, past ADR and migration text) and every `docs/history/` frozen document's own point-in-time wording; and narrowed the source policy's Clinical results section so a clinical source may support an independently established Program's phase, status, and sponsor intent, never establish a distinct Program from Study-level facts alone — the same registry-evidence-scope rule ADR-0055 already applies to trial registry citations, now applied identically to clinical-literature citations. Declined: relocating any of the three symmetric domain-root migration READMEs (Company/Pipeline, Clinical Evidence, and Application/UI each carry an identical, non-authoritative, self-declared-complete pattern; archiving only one would be inconsistent scope-creep since the other two were not investigated to the same depth); editing `overview_mock.html`'s stale version strings (a dead, unrouted file whose entire meta-strip is admittedly fabricated mock data, so correcting 2 of 6 fake values would misleadingly imply the mockup was reviewed for accuracy); building a second active-specification entrypoint or manifest (`AGENTS.md` already serves that role by design); and introducing an alignment CI check (no CI infrastructure exists in this repository at all, so adding the first one is a new infrastructure decision outside a documentation-governance pass). | The investigation found the Company/Pipeline–Clinical Evidence authority structure itself sound — `AGENTS.md` already functions as the version-agnostic active entrypoint, and every live active-version assertion for Contract 1.2 and Clinical Evidence Schema 3.1 was internally consistent — but surfaced defects accumulated from incremental edits: a markdown table silently broken by a non-adjacent blank line, dead links in a still-actively-maintained historical index, three probe scripts absent from every workflow's own checklist, and terminology drift between Company/Pipeline's "Contract" and Clinical Evidence's dual use of "Contract"/"Schema". None needed a structural move to fix. The domain-root README pattern and the CI-check question are recorded as real findings requiring a broader, explicitly scoped decision rather than folded into this pass. | Active | [Data Protocol versioning](./README.md#versioning), [Historical catalog](../../../docs/history/README.md) |
| ADR-0057 | Close the Phase C independent structural and schema gap audit (Asset–Program separation, add-on/background-therapy representation, field-level provenance, indication-vocabulary normalization, mechanism asset-level ownership, Program→Study reverse projection) with a layer-classify-first method: for each candidate, decide whether the true defect is Schema, Contract, Validator, Workflow, Projection, Consumer, or Shared model before evaluating it, then answer whether live data demonstrates a defect, whether a consumer/workflow requires it, whether the existing Contract/validator already solves it, and the migration cost. Result: **1 GO / 5 NO-GO / 1 already resolved**, of 7 candidates. Five classified Schema, Shared model, or Projection with **no demonstrated live defect** (re-verified directly against current `main`, not assumed carried-forward) and are NO-GO for now; asset-level-versus-program-level field ownership was already resolved by Phase A/B plus this GO, so it was not independently run through the 4-question test. The GO — `technical.mechanism` cross-row agreement — classified **Consumer**: `entities-and-rows.md` already states the correct rule (resolved mechanism family, not raw string), but `domains/app/lib/efficacy-comparison/mechanism-family.ts` still compared raw strings. Fixed the consumer function to compare resolved family IDs, extracting a pure `resolveMechanismFamilyFromMechanisms` core; added 3 synthetic self-checks to `scripts/probe-efficacy-comparison.mts` covering same-family-different-wording (must not throw), cross-family (must throw), and mixed disclosed/undisclosed (must throw), sourced from the live registry rather than hardcoded strings, as persistent regression coverage for this fix. Zero schema, registry, or data change. No `DEFERRED_SCHEMA_CASE` had accumulated under ADR-0055 to carry into this audit. | The layer-classification step exists because the Lilly failure (ADR-0055) presented as a schema gap and was actually Workflow — this audit tests whether that pattern repeats for the remaining candidates, and confirms it does: none of the five checked NO-GO candidates needed a schema change, because none had a demonstrated defect once checked directly rather than assumed. The one genuine defect found was a consumer implementing a stricter check than its own governing contract already specified: `sciwind-biosciences/ecnoglutide`'s two rows carry different published wording for the same molecule that both resolve to the same mechanism family, and the old raw-string equality would have thrown the moment this asset entered the Efficacy Comparison projection — proved directly against the live mechanism-families registry, not merely inferred. Verification precision: `lint` and `build` pass (exit 0) before and after; `data:probe:registry-citations` and `data:probe:mechanism-families` pass; `data:probe:efficacy-comparison` **exits 1 both before and after** on 8 pre-existing, unrelated fixture-staleness assertions (confirmed byte-identical failure text via `git stash` comparison) — the repository-wide suite was not fully green before this change and is not fully green after it, and this change is confirmed to add zero new failures to that count while adding 3 new passing self-checks. | Active | [Entities and Rows §Technical profile level](./entities-and-rows.md#technical-profile-level) |
| ADR-0058 | Revise the frozen Efficacy Comparison population coverage from 10-of-15 to 11-of-15 without changing the eligibility policy. | SCALE Obesity and Prediabetes directly reports liraglutide body-weight change as `-8.0%`; correcting the stored arm-level result from kg-only to that sourced percent metric makes liraglutide eligible under the existing strict gate. The snapshot also records 52 body-weight-outcome Studies, including one explicitly unclassified SUMMIT Study, while unit coverage remains an exhaustive 11 eligible / 4 dispositioned partition. | Active | [Clinical Evidence contract](../../clinical-evidence/docs/README.md#deferred-limitations), [UI Reference](../../app/docs/README.md#efficacy-comparison) |
| ADR-0059 | Add optional `officialWebsite`/`officialPipeline` dated reference links to the Company record. | The Company Detail UI will route out to a company's own official homepage/pipeline page; these are reference links to the company's own page, not evidentiary sources for a data claim, so they get a lighter `{ url, checkedAt }` shape rather than the full `source` citation shape. | Active | [Entities and Rows](./entities-and-rows.md#company) |
| ADR-0060 | Permit `administration.route`/`administration.dosageForm` to hold the sentinel `"Undisclosed"` instead of a real value, gated by the validator to rows whose `development.stage` sorts strictly before `Phase 3` in `development-stages.json` (Discovery through every Phase 1/Phase 2 label). Each field is gated independently, so a partially-disclosed candidate (for example, a sponsor stating "injectable" without naming the anatomical route) can store the disclosed field for real and `"Undisclosed"` only for the genuinely unstated one. | A Sciwind Biosciences refresh deferred three officially-named, in-scope, current pipeline candidates (XW016, XW019, XW020) solely because a route or dosage form was not yet disclosed — the "do not enter a partial record" rule, applied literally, discarded real pipeline data at exactly the stage (pre-IND) where a formulation is routinely still undecided. Gating to before-Phase-3 keeps the exception narrow: a route/dosage form is expected to be fixed and disclosed by the time a Phase 3 program runs, so the rule does not become a general escape hatch. | Active | [Source and Entry Policy](./source-and-entry-policy.md#undisclosed-routedosage-form), [Entities and Rows](./entities-and-rows.md#program-identity) |
| ADR-0061 | Decouple Clinical Evidence execution from Company/Pipeline Research. A Clinical Evidence run uses the checked-out Company/Pipeline source manifest only as a read-only identity/traversal reference, verifies its generated Company/Pipeline artifacts with `data:validate:company-pipeline:manifest`, writes only Clinical Evidence-owned generated files through `data:generate:clinical-evidence`, and never refreshes or repairs Company/Pipeline data. Missing, conflicting, source/generated-stale, or materially contradicted identity data is reported as a scoped blocker rather than being updated in place; manifest age alone is not a blocker without an expiry policy. The registry-citation probe remains advisory and its findings do not become automatic failures. | Clinical Evidence already has a one-way reference dependency on Company/Pipeline identity, but ADR-0035 and ADR-0054 coupled that dependency to a full same-run Company/Pipeline refresh, while the shared generator also rewrote all five generated files. The coupling caused a Clinical Evidence request to mutate another domain and made its success depend on unrelated Company/Pipeline source access. A separate persisted manifest was rejected because it would duplicate canonical identity and create a new drift surface; the source manifest plus a read-only byte-current check preserves one authority and makes stale generated identity fail without mutation. | Active | [Agent entry point](../../../AGENTS.md#research-routing), [Clinical Evidence workflow](../../clinical-evidence/docs/workflow.md), [Generated Output Contract](./generated-output-contract.md#validation-contract) |
| ADR-0062 | Clarify the Clinical Evidence indication-expansion boundary: MASH or another comorbidity does not by itself exclude a Study. A mixed-purpose Study is eligible when obesity, overweight, chronic weight management, or weight reduction is an explicit objective alongside the indication-expansion objective; only MASH-only and other indication-expansion Studies where body weight is incidental remain excluded. NCT06309992 (SYNCHRONIZE-MASLD) is the normative retained example because body-weight reduction and MASLD efficacy are explicit co-primary objectives. | The prior adjacent sentences said both `MASH-only` Studies were excluded and that "MASH and other indication expansion" remained outside scope, which could be read as excluding every Study that mentioned MASH even when weight reduction was independently central. The clarification preserves the intended body-weight-purpose boundary without treating a comorbidity label as an automatic exclusion. | Active | [Clinical Evidence contract](../../clinical-evidence/docs/README.md#evidence-scope) |
| ADR-0063 | Add an explicit, feature-local global-asset grouping to Efficacy Comparison. Confirmed cross-company versions of one molecule may be grouped only through authored membership, never by matching `assetId` or names. Olatorepatide groups the Hansoh regional and Regeneron global programs; eligible Regeneron/global evidence ranks before Hansoh/regional evidence, while the ordinary evidence ranking selects a regional fallback when no eligible global result exists. The row follows the winning Study and discloses its evidence company, authored sponsor label, Study region, and development-rights scope. | Company-local Program identity correctly represents territorial development responsibility, but using `companyId + assetId` directly as the Efficacy Comparison unit would render the same licensed molecule twice once both companies report results. Automatic cross-company merging was rejected because asset IDs are not globally authoritative. Explicit membership preserves Company/Pipeline ownership while giving the comparison surface one reviewed molecule-level row. | Active | [UI Reference](../../app/docs/README.md#efficacy-comparison) |
| ADR-0064 | Refresh the reviewed Efficacy Comparison population-coverage freeze to 15 eligible units of 24 evidence-bearing units, with 73 body-weight-outcome Studies, 5 missing Study population profiles, and 9 dispositioned gaps. The count uses the explicit global-asset grouping from ADR-0063, so Hansoh and Regeneron olatorepatide evidence contributes one comparison unit. No population, design, or metric eligibility rule is relaxed. | Clinical Evidence research added eligible and ineligible assets after ADR-0058's 11-of-15 snapshot. Retaining the old count would make a correct data refresh fail both readiness probes and would eventually double-count a licensed molecule when both territorial sponsors report outcomes. | Active | [Clinical Evidence Data Contract](../../clinical-evidence/docs/README.md), [UI Reference](../../app/docs/README.md#efficacy-comparison) |
| ADR-0065 | Refresh the reviewed Efficacy Comparison population-coverage freeze to 17 eligible units of 30 evidence-bearing units (from ADR-0064's 15-of-24), with 78 body-weight-outcome Studies, 5 missing Study population profiles, and 13 dispositioned gaps. A company-local comparison unit now also splits by `study.programId` in addition to company and asset, since Entities and Rows defines `programId` as company + asset + route + dosage form: Novo Nordisk semaglutide, Ascletis Pharma ASC30, Novo Nordisk amycretin, and Sciwind Biosciences ecnoglutide each split into a separate unit per route/dosage-form Program. No population, design, or metric eligibility rule is relaxed; the split is purely a grouping-key change in `collectUnits` (`domains/app/lib/efficacy-comparison/read-model.ts`) and its synchronized readiness-probe copy. | The prior asset-only key silently merged Studies from pharmacologically distinct Programs of one molecule — e.g. subcutaneous-injection and oral-tablet semaglutide, each with its own dose range and trial family — so the page's single-winner selection could pick one route's evidence and make the other route's evidence, and any coverage gap specific to it, invisible with no on-page indication that anything was hidden. | Active | [UI Reference](../../app/docs/README.md#efficacy-comparison), [Entities and Rows](./entities-and-rows.md#row-splitting) |
| ADR-0066 | Merge `primary` and `co-primary` into one top tier in the Efficacy Comparison representative-selection endpoint-role ranking (`getEndpointRoleRank`, `domains/app/lib/efficacy-comparison/policy.ts`), where they previously ranked as separate, ordered tiers. Changes the representative Study for exactly three units once every earlier ranking key (phase tier, endpoint role) ties and the ranking falls through to estimand: Novo Nordisk semaglutide's subcutaneous unit moves from STEP 8 to STEP 1, Eli Lilly tirzepatide moves from SURMOUNT-5 to SURMOUNT-1, and Novo Nordisk CagriSema moves from REDEFINE 4 to REDEFINE 1. Eligible/gap unit counts are unaffected — this changes only which already-eligible Study wins each of those three units, not whether a unit qualifies. | A trial that names body-weight change one of two co-primary endpoints (STEP 1 pairs it with a ≥5%-responder co-primary; SURMOUNT-1 and REDEFINE 1 do the same) is not weaker evidence on that endpoint than a trial naming it the sole primary — both are the trial's own prespecified confirmatory endpoint for the same metric. Ranking them as separate tiers let each asset's placebo-controlled, co-primary-designed pivotal trial systematically lose to an unrelated head-to-head trial for the same asset (STEP 8 vs. liraglutide, SURMOUNT-5 vs. semaglutide, REDEFINE 4 vs. tirzepatide) purely on this tie-break, which is not evidentiary superiority. | Active | [UI Reference](../../app/docs/README.md#efficacy-comparison) |
| ADR-0067 | Add required `Endpoint.assessmentTimepointWeeks` (weeks on study drug from first dose, or explicit `null`), mirroring `result.numericValue`'s value/machine-readable split applied to the timepoint. Backfilled all 362 existing Endpoint records by parsing the stored `assessmentTimepoint` text: a bare `"Week N"`, or a qualified form confirming drug and randomization start together (e.g. `"Week 72 of randomized treatment"` after a non-drug run-in, or `"Week 26 (Part A)"` naming one independently-dosed cohort of a two-part study), parses to `N`; a range or a mid-trial change window whose on-drug duration differs by arm (e.g. a randomized-withdrawal endpoint, or a compound timepoint like `"Week 68 (48 weeks after randomization)"`) is stored `null` rather than guessed — 2 of 185 body-weight, non-safety Endpoints landed `null` under this rule. | Efficacy Comparison's representative-result ranking orders candidates by phase tier then endpoint role tier only (ADR-0066), so within one study a shorter-duration primary endpoint always outranks a longer-duration secondary one (e.g. retatrutide's Phase 2 Week 24 primary result outranks its own Week 48 secondary result), and the Compare chart inherits the same ranking for any dose two candidates both report. A numeric, machine-readable duration is a prerequisite for any future ranking change that accounts for duration; whether and where to use it in the ranking order is intentionally deferred to a separate decision. | Active | [Clinical Evidence contract](../../clinical-evidence/docs/README.md) |
| ADR-0068 | Insert a duration tie-break into Efficacy Comparison's representative-selection ranking (`compareDurationTiebreak`, `domains/app/lib/efficacy-comparison/representative.ts`), between estimand and endpoint role: `phase tier → estimand → duration → endpoint role → analysis population → source completeness → maturity → curated order`. It fires only between two candidates that already share a genuine, reported estimand (`UNRANKED_ESTIMAND_RANK` — both merely absent — never counts as a match) and both carry a non-null `assessmentTimepointWeeks` (ADR-0067); when it fires, the longer on-drug timepoint wins regardless of role. Reused unmodified by the Compare chart's per-dose tie-break. Changes the representative Study for 5 units: retatrutide Phase 2 (Week 24 primary → Week 48 secondary), VISTA/elecoglipron (Week 26 co-primary → Week 36 secondary), and GLORY-1/mazdutide (Week 32 co-primary → Week 48 secondary) had a genuine role difference the old order let decide first; Novo Nordisk semaglutide's subcutaneous unit (STEP 1 → STEP 5) and oral unit (OASIS 4 → OASIS 1) had **matching role** across studies, where every key through the old role position already tied and the previous pick was decided by curated source order alone. Eligible/gap unit counts are unaffected. | Endpoint role states how much statistical protection a trial's own hierarchy gave one result, not how clinically informative that result's follow-up length is — retatrutide's Week 24 and Week 48 readouts share population, estimand, source, and maturity, differing only in which one the trial's own protocol pre-specified as confirmatory, so a longer, equally-well-estimated follow-up is more informative for a chronic-treatment comparison. Confirmed on the full dataset before adopting: the semaglutide STEP 1/STEP 5 and OASIS 1/OASIS 4 pairs each tied on every key up to the old role position (same phase, same merged role tier, same estimand, same population, same maturity, one source each) and were decided only by which Study happened to be entered into the source file earlier — an authoring-sequence artifact carrying no evidentiary weight, which the duration tie-break now replaces with an actual signal. Restricted to a shared *reported* estimand specifically to avoid the reverse failure mode: an earlier version considered without that restriction let two candidates that both simply omitted an estimand, or a genuinely lower analysis-rigor result, win purely on a longer timepoint — the guard keeps the tie-break from rewarding data that is longer but not equally well-estimated. | Active | [UI Reference](../../app/docs/README.md#efficacy-comparison), [Clinical Evidence contract](../../clinical-evidence/docs/README.md) |

New entries stay one row. Put the enforceable rule in its current authority and
use this index only to preserve the decision, short reason, status, and pointer.
